package use_cases.login_leaderboard;

/**
 * Register the user by creating a user object of the user.
 * If the user password is invalid (PasswordStrengthChecker), allow user to re-enter a password.
 * If the user password is valid, update the CSV with the user info, let the user know
 * they have been registered.
 */
public class RegisterUser {
    // Instance variable: Hashmap -> prev users
    // setUser -> sets hashmap of previous users

    // bool register -> check if username in hashmap
    // 1. create user object 2. add to hashmap 3. write to file

    //
}
