package hazards;

/** Interface for communicating relevant player information for hazard checking. */
public interface PlayerInfo {
    /** Get the x position of the player. */
    int getX();
    /** Get the y position of the player. */
    int getY();
}
