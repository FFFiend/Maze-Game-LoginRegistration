package user_interface.custom_game.custom_game_screens;

class CustomGameInitializerPanel implements ICustomGamePanel {
    //extends Screens

    /**
     * Displays the screen just before the editor to initialize the editing process
     */
    protected CustomGameInitializerPanel(){
        //use Screens' methods to draw and CustomGameScreenInterface for the features specific to custom game screens
    }
}