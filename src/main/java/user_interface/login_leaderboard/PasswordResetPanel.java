package user_interface.login_leaderboard;

import javax.swing.*;

/**
 * Sends the user an email with a code, if the code is correct, allows user to create a
 * new password. (Leave empty for now, we can add this feature if we have time).
 */
public class PasswordResetPanel extends Panel {

    public PasswordResetPanel() {
    }
}
